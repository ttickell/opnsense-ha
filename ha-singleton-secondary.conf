#!/bin/sh
# OPNsense HA Singleton Configuration File - Secondary Firewall Configuration
# 
# Network Topology:
# WAN: 192.168.50.0/24 (DHCP), fd03:17ac:e938:16::/64
# LAN: 192.168.51.0/24, fd03:17ac:e938:1f00::/64  
# Primary LAN: 192.168.51.2, fd03:17ac:e938:1f00::2
# Secondary LAN: 192.168.51.3, fd03:17ac:e938:1f00::3
# CARP VIP: 192.168.51.1, fd03:17ac:e938:1f00::1
# PFSYNC: 192.168.103.0/29

# WAN Interfaces to manage (space-delimited)
WAN_INTS="vtnet0_vlan110"

# Services to manage based on CARP status
SERVICES="rtsold dhcp6c radvd"

# Alternate default routes (when this secondary firewall becomes BACKUP)
# When this firewall is BACKUP, route through the PRIMARY firewall's LAN IP
ALT_DEFROUTE_IPV4="192.168.51.2"                    # Primary firewall's LAN IP
ALT_DEFROUTE_IPV6="fd03:17ac:e938:1f00::2"           # Primary firewall's IPv6 LAN IP

# Feature toggles
ENABLE_IPV6="yes"                    # Enable IPv6 management (test environment ready)
ENABLE_SERVICE_MANAGEMENT="yes"      # Enable service start/stop
ENABLE_ROUTE_MANAGEMENT="yes"        # Enable backup route management
DEBUG="yes"                         # Enable debug logging for testing

# IPv6 Integration settings
IPV6_SCRIPT_PATH="/usr/local/bin"    # Path to IPv6 management scripts
SYNC_IPV6_STATE="yes"               # Sync IPv6 state between nodes (when integrated)

# Advanced settings
INTERFACE_SETTLE_TIME="2"           # Seconds to wait after interface changes
SERVICE_STARTUP_DELAY="1"           # Seconds between service starts
HEALTH_CHECK_ENABLED="yes"          # Enable health checking